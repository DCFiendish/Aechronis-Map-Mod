# libs/

Nothing needs to go here — `build.gradle` resolves XaeroLib, Xaero's World Map,
and Xaero's Minimap directly from Maven repositories, so `./gradlew build`
works on a clean checkout with no manual steps.
